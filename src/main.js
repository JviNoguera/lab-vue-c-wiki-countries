import "bootstrap/dist/css/bootstrap.css"; // IT    0 - LAB | Vue.js WikiCountries (Composition API)
import { createApp } from "vue";
import App from "./App.vue";
import router from "./router"; // IT    0 - LAB | Vue.js WikiCountries (Composition API)


createApp(App).use(router).mount("#app");